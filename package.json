{
  "name": "codex-process-jobs",
  "version": "0.1.0",
  "description": "Portable detached process job runtime for Codex",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "codex-process-jobs": "./scripts/job.mjs"
  },
  "scripts": {
    "check": "node --check scripts/job.mjs && node --check scripts/worker.mjs && node --check scripts/notifier.mjs && node --check scripts/install.mjs && node --check scripts/smoke.mjs && node --check hooks/unread-result-hook.mjs && node --test",
    "smoke": "node scripts/smoke.mjs",
    "test": "node --test"
  }
}
